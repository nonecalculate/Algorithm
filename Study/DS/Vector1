#include<iostream>
#include<vector>

using namespace std;
int main() {
	vector<int> v;
	for (int i = 0; i < 100; i++)
	{
		cout << "size=" << v.size() << " capacity=" << v.capacity() << "\n";
		v.push_back(i);
	}
}
